{% extends "AevitasLevisBundle:Front:root.html.twig" %}
{% block title %}
<title>{{'User Dashboard' | trans}}</title>
{% endblock %}
{% block header %}
<!-- Le styles -->
{% stylesheets output='bundles/aevitaslevis/front/cpassets/compress_dashboard.css' filter='yui_css'
    '@AevitasLevisBundle/Resources/public/front/css/bootstrap.css'
    '@AevitasLevisBundle/Resources/public/front/css/bootstrap-responsive.css'
    '@AevitasLevisBundle/Resources/public/front/css/select2.css'
    '@AevitasLevisBundle/Resources/public/front/css/datepicker.css'
    '@AevitasLevisBundle/Resources/public/front/css/style.css'
    '@AevitasLevisBundle/Resources/public/front/css/reward.css'
    '@AevitasLevisBundle/Resources/public/front/css/reward_detail.css'
    '@AevitasLevisBundle/Resources/public/front/css/account.css'
    '@AevitasLevisBundle/Resources/public/front/css/clock.css'
%}
<link rel="stylesheet" href="{{ asset_url }}" type="text/css" media="screen" />
{% endstylesheets %}
{% endblock %}
{% block content %}

<div id="breadcrums">
    <ul>
        <li onclick="void(0);" class="br-home"></li>
        <li class="br-arrow"></li>
        <li class="br-normal"><a href="{{path('levis_user_dashboard')}}">{{'Customer Account' | trans}}</a></li>
        <li class="br-arrow2"></li>
        <li class="br-arrow"></li>
        <li class="br-normal"><a href="javascript:void(0);">Dashboard</a></li>
        <li class="br-arrow2"></li></ul>
</div>
<!-- Registration Content
================================================== -->
<!-- Wrap of all the registration block. -->
<section id="userdash" class="clearfix">
    <div class="shadow_wrapper">
        <div>
            <div class="content">
                <div class="info">
                    <ul class="topnav">
                        <li class="active"><a href="{{path('levis_user_dashboard')}}">{{'Dashboard' | trans}}</a></li>
                        <li><a href="{{path('levis_user_profile')}}">{{'Profile' | trans}}</a></li>
                        <li><a href="{{path('levis_user_activity')}}">{{'Point History' | trans}}</a></li>
                        <li><a href="{{path('levis_user_shopping')}}">{{'Shopping History' | trans}}</a></li>
                    </ul>
                    <section>
                        <div class="table yellow">
                            <div>
                                <h3>{{'Card ID' | trans }}: {{user.getCCode}}</h3>
                                <span>{{'Point Balance'| trans}}: {{user.getPoint}} - {{'Tier' | trans}}: {{user.getLevel}}</span>
                            </div>
                            <!--div class="body static">
                                {% if nextlevel > 0 %}
                                <strong>{% trans with {'%point%' : nextlevel} %}Hello! You need %point% purchase point to upgrade to the next level{% endtrans %}</strong>
                                {% elseif nextlevel == 0 %}
                                <strong>{{'You need at least 50 purchase points to be an official Star Club member! Make a purchase at any Levis store near you!' | trans}}</strong>
                                {% else %}
                                <strong>{{'Congratulations! You are a Platinum member of Star Club.' | trans}}</strong>
                                {% endif %}
                                <div>
                                    <div class="bar">
                                        <div class="points" style="width:{{user.getPoint*100/500}}%"></div><span id="ttpoint" data-toggle="tooltip" title="{{'Your current PURCHASE POINTS' | trans}}: {{user.getPoint}}"></span>
                                    </div>
                                        <span>0</span><span>1000</span><span>2000</span><span>3000</span><span class="postend">4000</span><span>5000</span>
                                </div>
                                <div class="level">
                                    <div class="point">{{'Points' | trans}}</div>
                                    <div class="point tier">{{'Tiers' | trans}}</div>
                                    <div><span>{{'Silver' | trans}}</span><span class="gold">{{'Gold' |trans}}</span><span class="platin">{{"Platinum" | trans}}</span></div>
                                </div>
                            </div-->
                            <div class="bottom"><div><div></div></div></div>
                        </div>
                        <!--div class="nextgrade">
                            {# for clock in clocks %}
                                <div class="clockitem">
                                    {% if user.getPoint < clock.getPoint %}
                                    <h3 class="avro">{% trans with {'%points%': (clock.getPoint - user.getPoint), '%gift%': clock.getName} %}Get %points% points to redeem %gift%{% endtrans %}</h3>
                                    <div class="gift">
                                        <a href="{{path('levis_gift_detail', {'slug': clock.getSlug, 'topcat': clock.getCat, 'category': clock.getCategory.getSlug~'-'~clock.getCatid, '_format': 'html'})}}" title="{{clock.getName}}"><img src="{{asset(clock.getThumbnail(148,148))}}" width="200" alt="{{clock.getName}}"/></a>
                                        <a href="{{path('levis_gift_detail', {'slug': clock.getSlug, 'topcat': clock.getCat, 'category': clock.getCategory.getSlug~'-'~clock.getCatid, '_format': 'html'})}}" title="{{clock.getName}}" class="yellow_button small">{{'Check out' | trans}}</a><a href="{{ path('levis_gift_addwishlist', {'gid' :clock.getId}) }}" class="addwishlist yellow_button small" id="gift-{{clock.getId}}">{{'Add to wishlist' | trans}}</a>
                                    </div>
                                    {% else %}
                                    <h3 class="avro">{% trans with {'%points%': (clock.getPoint - user.getPoint), '%gift%': clock.getName} %}Able to redeem %gift%{% endtrans %}</h3>
                                    <div class="gift">
                                        <a href="{{path('levis_gift_detail', {'slug': clock.getSlug, 'topcat': clock.getCat, 'category': clock.getCategory.getSlug~'-'~clock.getCatid, '_format': 'html'})}}" title="{{clock.getName}}"><img src="{{asset(clock.getThumbnail(148,148))}}" width="200" alt="{{clock.getName}}"/></a>
                                        <a href="{{path('levis_gift_detail', {'slug': clock.getSlug, 'topcat': clock.getCat, 'category': clock.getCategory.getSlug~'-'~clock.getCatid, '_format': 'html'})}}" title="{{clock.getName}}" class="yellow_button small">{{'Check out' | trans}}</a><a href="{{ path('levis_gift_addwishlist', {'gid' :clock.getId}) }}" class="addwishlist yellow_button small" id="gift-{{clock.getId}}">{{'Add to wishlist' | trans}}</a>
                                    </div>
                                    {% endif %}
                                </div>
                            {% endfor #}
                            </div-->
                            <div class="row">
                                <div class="table gray">
                                            <div>
                                                <h3>{{'Account Summary' | trans}}</h3>
                                            </div>
                                            <div class="body">
                                                <label>{{'Your current tier' | trans}}:</label><strong>{{user.getLevel}}</strong>
                                                <label>{{'Earned Point' | trans}}:</label><strong>{{user.getPoint}}</strong>
                                                <label>{{'Level Rank' | trans}}:</label><strong>{{user.getQualifyPoint}}</strong>
                                                <label>{{'Your tier' | trans}}:</label><strong>{{user.getLevel}}</strong>
                                                <div class="floatbt"><a class="yellow_button" href="{{path('levis_user_profile')}}">{{'Edit Profile' | trans}}</a></div>
                                            </div>
                                            <div class="bottom"><div><div></div></div></div>
                                        </div>
                                <div class="table gray">
                                    <div>
                                        <h3>{{'Your Profile' | trans}}</h3>
                                    </div>
                                    <div class="body">
                                        <label>{{'Email' | trans}}:</label><strong>{{user.getEmail}}</strong>
                                        <label>{{'Mobile' | trans}}:</label><strong>{{user.getCellphone}}</strong>
                                        <label>{{'Firstname' | trans}}:</label><strong>{{user.getFirstname}}</strong>
                                        <label>{{'Lastname' | trans}}:</label><strong>{{user.getLastname}}</strong>
                                        <label>{{'Address' | trans}}:</label><strong>{{user.getAddress1}}</strong>
                                    </div>
                                    <div class="bottom"><div><div></div></div></div>
                                </div>
                                <!--div class="table gray wishlist">
                                    <div>
                                        <h3>Your Wishlist</h3>
                                    </div>
                                    <div class="body">
                                        <ul class="wishlist">
                                        {# for gift in wishlist %}
                                                <li><img src="{{asset(gift.getThumbnail(75,75))}}" width="75" height="75"/></li>
                                        {% endfor #}
                                                </ul>
                                                <div class="floatbt"><button class="yellow_button">{{'View Your Wishlist' | trans}}</button></div>
                                            </div>
                                            <div class="bottom"><div><div></div></div></div>
                                        </div-->
                                    </div>
                                </section>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
{% endblock %}
{% block javascript %}
            var wishlist = [{{user.getWishlistIds | join(',')}}];
            for(i = 0; i < wishlist.length; i++){
                $('#gift-'+wishlist[i]).hide();
            }
            $('#ttpoint').tooltip('show');
            $('.addwishlist').click(function(e) {
                e.preventDefault();
                $.ajax({
                    url: $(this).attr('href'),
                    type: "POST",
                    dataType: "json",
                    success: function(response) {
                        //console.log(response);
                        if (typeof(response.error) != 'undefined') {
                            //failed, gift not exists.
                            if (response.error == 'exists') {
                                alert('this gift already exists in your wish list!');
                            }
                            return;
                        }
                        if (typeof(response.result) != 'undefined' && response.result == true) {
                            alert('this gift was added to your wish list!');
                        }
                    }
                });
            });
{% endblock %}
